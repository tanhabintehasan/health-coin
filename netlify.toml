[build]
  command = "npm install && npm run build:admin"
  publish = "apps/admin-web/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect: send all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
